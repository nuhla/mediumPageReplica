const mongoose = require("mongoose");
const oldData = require("./dbgenaratin").oldData;
const Articl = require("./database/models/Article").Article;
// const mongoose2 = require("mongoose");
console.log(oldData);
